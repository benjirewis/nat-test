> **Warning:** This code is a WIP that may disappear at any second.

# nat-test

Does basic characterization of your network. Ensure `go` is installed. Use `make` to run.
Output is not guaranteed to be easy to read.
